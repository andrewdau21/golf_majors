# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Real entry data — never committed
data/USOpen.csv       # raw picks CSV (source of truth, local only)
src/data/entries.js   # generated at build time, local only
# data/entries.enc is intentionally tracked — it is AES-256-GCM encrypted

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
